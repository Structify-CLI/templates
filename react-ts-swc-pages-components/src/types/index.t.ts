/**
 * Shared TypeScript types and interfaces for React pages and components.
 *
 * @fileoverview
 *   Place all common types, interfaces, and type utilities here for reuse.
 *
 * @example
 *   interface User {
 *     id: number;
 *     name: string;
 *     email: string;
 *   }
 *
 * @usage
 *   Import and use your interfaces/types in components:
 *   import type { User } from '../types';
 *
 * @params
 *   - None (this file is for type definitions only)
 */
